# BEATERRA OS Backend

API REST Node.js + TypeScript sous `/api/v1`. Copiez `.env.example` vers `.env` et définissez les secrets hors du dépôt.

Les règles financières et métier inter-tables sont appliquées par les services conformément à `../database/SERVICE_VALIDATION_RULES.md`.
